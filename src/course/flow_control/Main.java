package course.flow_control;

public class Main {
    public static void main(String[] args) {
        // if...else
            // if(statement to be validated) {
            //      response if true
            // } else {
            //      response if false
            // }
        //boolean isTrue = 1 > 2 ? true : false;
        //boolean isTrue = 1 > 2;


        // switch
            // switch (expression) {
            //      case constant1 : code block
            //      case constant2 : code block
            //      case constant3 : code block
            //      default : code block
            // }

        // loop
        // for loop
            // for (int i = 0; i < condition; i++){}
            // for (int i = 0; i > condition; i++){}
            // for (int i = 0; i == condition; i++){}
            // ...
            // for enhanced
                // for ( String varName : someArray )

        // while
        // while (someCondition) { runs code until condition turns invalid }
    }
}
